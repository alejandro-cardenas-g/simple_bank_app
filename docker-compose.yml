services:
  postgres:
    image: postgres:14.3
    ports:
      - "5439:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mydb
    container_name: pg_go
    volumes:
      - ./postgres:/var/lib/postgresql/data
